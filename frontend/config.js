/* Configuration for the bias detection tool */

const CONFIG = {
    // App settings
    appName: 'AI Bias Detection Tool',
    version: '1.0.0',
    
    // API settings
    // For local development: 'http://127.0.0.1:8000'
    // For production: Update this to your deployed backend URL
    apiUrl: 'http://127.0.0.1:8000',
    
    // UI settings
    loadingDelay: 1500, // milliseconds
    animationSpeed: 300, // milliseconds
};